// conf/modules_assembly.config — selectors for assembly + annotation + stats

process {

    // ── ASSEMBLY ──
    withName: 'UNICYCLER' {
        ext.args = '--mode bold'
        publishDir = [
            path: { "${params.outdir}/assembly/${meta.id}" },
            mode: 'copy'
        ]
    }

    // ── ANNOTATION ──
    withName: 'PROKKA' {
        publishDir = [
            path: { "${params.outdir}/annotation/prokka/${meta.id}" },
            mode: 'copy',
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]
    }
    withName: 'PHAROKKA_PHAROKKA' {
        publishDir = [
            path: { "${params.outdir}/annotation/pharokka/${meta.id}" },
            mode: 'copy',
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]
    }

    // ── MAPPING TO SELF ──
    withName: '.*PREPARE_ASSEMBLY.*BWA_INDEX' {
        memory = { 4.GB * task.attempt }
    }
    withName: '.*PREPARE_ASSEMBLY.*BWA_MEM' {
        ext.args = { "-M -R '@RG\\tID:${meta.id}\\tSM:${meta.id}\\tLB:${meta.id}\\tPL:ILLUMINA'" }
        publishDir = [
            path: { "${params.outdir}/mapping/self/${meta.id}" },
            mode: 'copy',
            pattern: '*.bam'
        ]
    }
    withName: '.*PREPARE_ASSEMBLY.*SAMTOOLS_INDEX' {
        publishDir = [
            path: { "${params.outdir}/mapping/self/${meta.id}" },
            mode: 'copy',
            pattern: '*.bai'
        ]
    }

    // ── STATS ──
    withName: 'PICARD_COLLECTWGSMETRICS' {
        ext.args = '--COVERAGE_CAP 100000 --COUNT_UNPAIRED true'
    }
    withName: 'QUAST' {
        publishDir = [
            path: { "${params.outdir}/quast/${meta.id}" },
            mode: 'copy',
            saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
        ]
    }
    withName: 'CONTIG_ABUNDANCE' {
        publishDir = [
            path: { "${params.outdir}/stats/contigs/${meta.id}" },
            mode: 'copy'
        ]
    }
}
